#!/usr/bin/make -f
# See debhelper(7) (uncomment to enable)
# output every command that modifies files on the build system.
#export DH_VERBOSE = 1

export PYBUILD_NAME=ec3po
export PYBUILD_SYSTEM=distutils

# Build with only python2 for now.
# The ec module can do python2+3 now, but also installs tools that
# require python2.
# The python3-migration in chromeos seems to be stalled as of 20230416.

%:
	dh $@ --with python2 --buildsystem=pybuild
#	dh $@ --with python2,python3 --buildsystem=pybuild


#override_dh_auto_build:
#	make -j1
#	pybuild --build -i python{version} -p 2.7 --dir .


# dh_make generated override targets
# This is example for Cmake (See https://bugs.debian.org/641051 )
#override_dh_auto_configure:
#	dh_auto_configure -- #	-DCMAKE_LIBRARY_PATH=$(DEB_HOST_MULTIARCH)

